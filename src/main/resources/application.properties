spring.application.name=ProjectX

# Port (optional if you want a specific port for testing)
server.port=8080

# Disable the banner during tests (optional, just for clarity)
spring.main.banner-mode=off
